# Fiches techniques

Put product spec sheets (PDF) in this folder, then reference them from the
admin dashboard (`admin.html`) or directly in `tools/data/products.json`:

```json
"datasheet": { "file": "assets/docs/store-enrouleur-screen.pdf", "label": "Fiche technique (PDF)" }
```

Keep filenames lowercase, without spaces or accents.
